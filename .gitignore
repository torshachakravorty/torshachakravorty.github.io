_site/

# Local build / dev artifacts (machine-specific — never commit)
.bundle/
.jekyll-cache/
.jekyll-metadata

# Atomic-write leftovers
*.tmp.*
# Files downloaded from the Claude conversation — not part of the site
Claude outputs/
